/**
 * @deprecated This file has been consolidated into types.ts
 * All types are now exported from types.ts for better organization.
 */

// Re-export from types.ts for backward compatibility
export type {
  CortiMessageMetadata,
  CortiMessageData,
  CortiUIMessage,
  CortiUIMessageChunk,
} from './types.js';
